ifeq ($(strip $(METIS_CC)),)
    CC = icc
else
    CC = $(METIS_CC)
endif

# Which compiler to use
# esto es para los mics
# CC = icc -mmic

# What optimization level to use
OPTFLAGS = -O3 

# What options to be used by the compiler
COPTIONS =  -DI4 

# What options to be used by the loader
# Bea dice que paar poder compilar tu propio metis en nord2 hay que usar LDOPTIONS = -DI4 -m64
·LDOPTIONS = -DI4

# What archiving to use
AR = ar rv

# What to use for indexing the archive
RANLIB = ranlib
#RANLIB = ar -ts
#RANLIB = 


